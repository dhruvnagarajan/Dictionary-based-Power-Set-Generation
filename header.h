#include <bits/stdc++.h>
using namespace std;

#define __n(i) int one_d = i
#define __m(i) int two_d = i
#define FOR(i, n) for (int i = 0; i < n; i ++)
#define cFOR(i, l, n) for (int i = l; i < n; i ++)
#define crFOR(i, l, n) for (int i = n - 1; i >= l; i --)
#define rFOR(i, n) for (int i = n - 1; i >= 0; i --)
#define sout(msg) cout << msg << endl
#define db(msg, err, nl) cout << ":: " << msg << " @ " << err; nl ? cout << endl: cout << "\t"
#define pb(ele) push_back (ele)
#define start int main () {
#define trats return 0; }
#define end return 0; }
#define xdee(file) freopen (file, "r", stdin); int t; cin >> t; while (t --) {
#define xend }
#define eedx }